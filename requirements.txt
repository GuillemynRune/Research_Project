# Local Models Requirements - Gemma 3 VLM Edition
# Add these to your existing requirements.txt or install separately

# Speech-to-Text (Whisper)
openai-whisper>=20231117

# Vision-Language Model (Gemma 3)
torch>=2.1.0
transformers>=4.38.0
accelerate>=0.26.0
pillow>=10.0.0

# Optional: 8-bit quantization (saves 50% VRAM)
bitsandbytes>=0.41.0

# HuggingFace Hub (for model downloads)
huggingface-hub>=0.20.0

# Audio processing (likely already in base requirements)
numpy>=1.24.0
scipy>=1.10.0

# TTS Options (choose one or none)
# Option 1: Piper TTS (Recommended - fast and good quality)
piper-tts>=1.2.0

# Option 2: Coqui TTS (More voices, slower)
# TTS>=0.22.0

# Option 3: Bark (High quality, very slow)
# git+https://github.com/suno-ai/bark.git

# YOLO for face tracking (if using --head-tracker yolo)
# Already in [yolo_vision] extra:
ultralytics>=8.0.0
supervision>=0.16.0